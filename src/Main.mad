import IO from "IO"
import Math from "Math"

import Dungeon from "./Dungeon"
import Vector from "./Vector"



main = () => {
  IO.put("0123456789")
  pipe(
    Dungeon.render,
    IO.put,
  )(Dungeon.createDungeon("swapjabble", 20, 40))
}
